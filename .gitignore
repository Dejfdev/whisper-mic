# macOS files
.DS_Store
.AppleDouble
.LSOverride

# Build outputs
*.app/
*.o
*.out
/WhisperMic
AppIcon.iconset/

# Exclude SnipFlow folder entirely (it has its own repository now)
snipflow/

# Local configuration and private tokens
.agents/
.env*
mcp_config.json
